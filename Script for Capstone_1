-- This script will be paraphrased due to the lengthiness of it, but this is explaining the functions used and the data that I managed to obtain through the queries I created. -- 

Good afternoon everyone, 
I will be briefly explaining my queries and the analysis I have made regarding my sales territory, New Jersey, the region, which is the Northeast, and my recommendation for where EmporiUm to ensure that they are succeeding within all areas.

For my the first business question that I was tasked with answering, which was calculating the total overall revenue for the sales territory along with the start and end dates. For this first question I used the minimum (MIN) and maximum (MAX) functions in order to obtain the start and end date, then I used the sum (SUM) function in order to obtain the overall revenue. The table used for this was mostly the store_sales tables and I even made sure to have a where (WHERE) clause to specify, which sales territory I was pulling the information for. 

For the second question, which was calculating the month-by-month revenue for New Jersey. For this I used the Transaction_Date column twice with an alias of 'ss', which specified that the information was coming from the store_sales table, and I used the Month (MONTH) and Year (YEAR) functions to obtain the months and the year. The main table that I used was the store_list table, which has an alias of 'stl' in order to not confuse my self or SQL when it comes to joining tables. As you can see, I was able to obtain the information that was needed for my analysis. 

For the third question, I was tasked with providing the total revenue for the sales territory and the region it belongs to. For this, I used the store_managers tables joined the store_locations and store_sales tables to it as well. I even used the union (UNION) function to bring the two select columns together rather than leaving them as two different select (SELECT) statements that would run separately rather than concurrently. I also made sure to have sm.Region and sm.State labeled as 'Location' in order to not have both the region and the state labelled as "Region" altogether. 

For my fourth table, I was tasked with calculating the number of transactions per month and average transaction size by product category for the territory. In order to do so, I had written a query that connected the store_sales table, products, and inventory_categories table to each other with store_sales being the main table that the others were joined to. For this query, I used the count (COUNT), Month (MONTH), Year (YEAR), Round (ROUND), and Average (AVG) functions in order to obtain the data.

As for my fifth and final task, I had used the store_managers table while also joining store_locations and store_sales tables to it by using Store_ID for store_sales and State for store_locations. The join for State from the store_locations table was a little tricky for me because that was completely new and I had to get out of the mindset that I couldn't use that column at all for my joins. The functions I used included SUM and then the RANK() OVER (ORDER BY...SUM(...) DESC) function. This query while difficult did show me that technology and acessories are the highest selling merchandise that the company has. 

Here is my pie chart for the rankings of sales performance by state, which shows that Maine is the smallest section compared to the others, and then here is my bar chart showing the highest selling category, which is technology and accessories. 

My recommendation overall for the business would be to focus on the stores within Maine in the next quarter so that their revenue can begin to rival the other stores and selling technology and acessories is a good way to ensure their total overall revenue becomes one of the highest within the region. 
